# Environment
NODE_ENV=development
PORT=3000

# Database (Prisma) - MySQL example
# DATABASE_URL="mysql://DB_USER:DB_PASS@localhost:3306/DB_NAME"
# DATABASE_URL="mysql://DB_USER:u286264826_ROOTQndAProd@srv1508.hstgr.io/u286264826_QndAProd"

# Optional shadow DB for migrations (e.g., PlanetScale)
# SHADOW_DATABASE_URL="mysql://SHADOW_USER:SHADOW_PASS@localhost:3306/SHADOW_DB"

# JWT / Auth
ACCESS_TOKEN_SECRET=replace_with_strong_random_string
REFRESH_TOKEN_SECRET=replace_with_strong_random_string
ACCESS_TOKEN_EXPIRES_IN=15m
REFRESH_TOKEN_EXPIRES_IN=7d
BCRYPT_SALT_ROUNDS=10

# Admin bootstrap (used by seed script or initial setup)
MASTER_ADMIN_EMAIL=admin@example.com
MASTER_ADMIN_PASSWORD=ChangeMeStrong!23

# CORS / Frontend
FRONTEND_URL=http://localhost:3000
CORS_ORIGIN=http://localhost:3000

# Email / SMTP (optional)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_FROM="no-reply@example.com"

# Optional monitoring / cache
SENTRY_DSN=
REDIS_URL=redis://localhost:6379

# Any other app-specific secrets
# EXTRA_SECRET=